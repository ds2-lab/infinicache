Source: https://cloud.google.com/cloud-build/docs/quickstart-build

# find your project id
$ gcloud config get-value project

# build and push on gc registry
$ gcloud builds submit --tag gcr.io/project-id/image

# Create a Service object that exposes the deployment:
$ kubectl expose deployment deployment-name --type=LoadBalancer --name=service-name

# Display information about the Service:
$ kubectl get services service-name
